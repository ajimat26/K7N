```text
k7n-killer-vosw/
├── app.py                    # Backend Flask (eksekusi nyata)
├── requirements.txt          # Dependencies
├── static/
│   └── uploads/             # Folder untuk script yang diupload
├── templates/
│   └── index.html           # Frontend lengkap (gabungan)
└── run.sh                   # Script untuk menjalankan (opsional)
```
